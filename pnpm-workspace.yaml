packages:
  - docs
onlyBuiltDependencies:
  - esbuild

# Security: VitePress 1.x pins vite 5 / esbuild 0.21, which have dev-server advisories.
# Remove once VitePress 2 is stable.
overrides:
  vite: ^6.4.3
