{
  "name": "restassured-docs",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-adrs.mjs",
    "dev": "pnpm sync && vitepress dev",
    "build": "pnpm sync && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "mermaid": "^11.12.0",
    "vitepress": "^1.6.4",
    "vitepress-plugin-mermaid": "^2.0.17"
  }
}
